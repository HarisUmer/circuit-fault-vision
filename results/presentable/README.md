# Presentable results

Open **`index.html`** in a browser. That is the gallery.

This folder is the show pack. Training dumps stay in `results/investor_proof`, `results/circuit_faults`, `results/home_faults`, and `results/enhance_proof`.

| File | What it is |
|------|------------|
| `index.html` | Gallery + numbers (open this) |
| `FINDINGS.md` | Same facts in markdown |
| `images/` | Named stills for slides or email |

**Do not present** synthetic home-wires drawings as the home-circuit demo. **Do not** lead with `13_why_not_complete_class.jpg` except to explain why we dropped the `complete` class. **Never print “safe.”**
